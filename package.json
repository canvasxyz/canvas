{
  "name": "canvas",
  "private": true,
  "version": "0.0.36",
  "type": "module",
  "scripts": {
    "dev": "tsc --build --watch",
    "build": "tsc --build && npm run build --workspace=@canvas-js/example-chat-next --workspace=@canvas-js/example-chat-webpack",
    "clean": "tsc --build --clean",
    "version": "npm version --git-tag-version=false --workspaces=true --include-workspace-root=true --workspaces-update=true --save --save-exact",
    "publish": "npm publish --workspace=packages --access public",
    "test": "npm run test --workspace=@canvas-js/core"
  },
  "workspaces": [
    "packages/interfaces",
    "packages/core",
    "packages/cli",
    "packages/next",
    "packages/hooks",
    "packages/bootstrap-peer",
    "packages/signers",
    "packages/verifiers",
    "examples/app-store",
    "examples/chat-webpack",
    "examples/chat-next"
  ],
  "dependencies": {
    "wagmi": "^0.10.0"
  }
}
