{
  "name": "canvas",
  "private": true,
  "version": "0.0.47",
  "type": "module",
  "scripts": {
    "dev": "tsc --build --watch",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "publish": "npm publish --workspace=packages --access public",
    "test": "npm run test --workspace=@canvas-js/core --workspace=@canvas-js/signers"
  },
  "workspaces": [
    "packages/interfaces",
    "packages/core",
    "packages/cli",
    "packages/next",
    "packages/hooks",
    "packages/signers",
    "packages/verifiers",
    "examples/app-store",
    "examples/chat-webpack",
    "examples/chat-next"
  ]
}
