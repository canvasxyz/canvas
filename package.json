{
  "name": "canvas",
  "private": true,
  "version": "0.6.0-alpha7",
  "type": "module",
  "scripts": {
    "dev": "tsc --build --watch",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "lint": "eslint .",
    "publish": "npm run build && npm publish --workspace=packages --access public",
    "test": "npm run test --workspace=@canvas-js/core --workspace=@canvas-js/interfaces --workspace=@canvas-js/gossiplog --workspace=@canvas-js/vm --workspace=@canvas-js/signed-cid --workspace=@canvas-js/modeldb",
    "chain-test": "npm run test --workspace=@canvas-js/interfaces"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "@ava/typescript": "^4.1.0",
    "@types/node": "^20.8.5",
    "@typescript-eslint/eslint-plugin": "^6.7.5",
    "@typescript-eslint/parser": "^6.7.5",
    "ava": "^5.3.1",
    "eslint": "^8.51.0",
    "eslint-plugin-import": "^2.28.1",
    "typescript": "^5.2.2"
  }
}
