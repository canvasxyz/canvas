{
  "name": "canvas",
  "private": true,
  "version": "v0.0.50-pre2",
  "type": "module",
  "scripts": {
    "dev": "tsc --build --watch",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "publish": "npm run build && npm publish --workspace=packages --access public --tag beta",
    "test": "npm run test --workspace=@canvas-js/core"
  },
  "workspaces": [
    "packages/interfaces",
    "packages/core",
    "packages/cli",
    "packages/next",
    "packages/hooks",
    "packages/chain-ethereum",
    "examples/app-store",
    "examples/chat-webpack",
    "examples/chat-next"
  ]
}
