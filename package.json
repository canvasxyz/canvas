{
  "name": "canvas",
  "private": true,
  "version": "0.0.23",
  "type": "module",
  "scripts": {
    "appstore": "npm run dev --workspace=@canvas-js/example-app-store",
    "dev-example-chat": "GENERATE_SOURCEMAP=false npm run dev --workspace=@canvas-js/example-chat-client",
    "dev": "tsc --build --watch",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "version": "npm version --git-tag-version=false --workspaces=true --include-workspace-root=true --workspaces-update=true --save --save-exact",
    "publish": "npm publish --workspace=packages --access public",
    "test": "npm run test --workspace=@canvas-js/core"
  },
  "workspaces": [
    "packages/interfaces",
    "packages/core",
    "packages/cli",
    "packages/next",
    "packages/hooks",
    "packages/bootstrap-peer",
    "examples/app-store",
    "examples/chat-webpack",
    "examples/chat-next"
  ]
}
