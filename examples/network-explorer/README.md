# Network Explorer Example

## Usage

```
npm run dev:server
npm run dev:client
```

## Configuration

env vars:

- PORT (default 3000)
- DATABASE_URL
- NODE_ENV

server.ts:

- TOPICS

(todo: move to config.ts)
