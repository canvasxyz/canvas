# canvas-chat-browser

### Deployment

Set up CLI for Cloudflare Pages:

```
npm install -g wrangler
```

Build:

```
npm run build
```

Deploy:

```
wrangler login
npx wrangler pages publish dist
```
