import React from "react";
import { Room } from "../../shared/index.js";
export interface RoomNameProps {
    room: Room;
}
export declare const RoomName: ({ room }: RoomNameProps) => React.JSX.Element | null;
